## 1. 持久化规则状态与迁移

- [x] 1.1 定义当前清理规则、当前全部桌面规则及手动/自动/旧规则快照的版本化统一规则状态模型和原子仓库；验证无效状态或无效正则表达式会被拒绝且不替换现有状态文件。
- [x] 1.2 在统一状态服务后改造清理锁定持久化，同时保留现有公开清理行为；验证此前保存的精确或正则清理锁定在应用重启后仍受保护。
- [x] 1.3 添加从现有清理锁定和桌面布局快照文件到统一状态的首次加载迁移，将旧布局转换为旧规则快照且不删除源文件；验证升级会保留旧桌面规则和当前清理锁定。
- [x] 1.4 添加向后兼容的自动快照默认设置：关闭、30 分钟间隔和保留 3 个自动快照；验证默认值可见但不启用捕获。

## 2. 全局桌面规则

- [x] 2.1 从快照所属记录提取可复用的桌面规则身份、验证、保守匹配、预览和应用行为；验证唯一匹配窗口仅在预览确认后移动，缺失和歧义候选保持不动。
- [x] 2.2 通过双向唯一性检查实现全部桌面规则同步；验证移动后的正则匹配窗口仅更新目标桌面，未匹配窗口新增精确规则，已关闭窗口的规则保留，歧义映射被报告且不修改。
- [x] 2.3 添加全局桌面规则列表、编辑、新增、删除和正则表达式验证操作；验证无论当前活动桌面为何，完整集合均可编辑。
- [x] 2.4 确保应用桌面规则仅创建缺失目标桌面，且不改变清理锁定状态、不启动或关闭应用；通过现有恢复式结果报告验证全部结果。

## 3. 窗口管理器与快照管理界面

- [x] 3.1 以内置窗口管理器右下角固定的 `更新桌面规则`、`管理桌面规则` 和 `应用桌面规则` 替换快照所属规则操作；验证每个操作均处理所有已识别桌面的规则/窗口而不是活动桌面。
- [x] 3.2 将快照管理窗体重构为规则快照历史视图，支持手动捕获、查看、重命名、删除、恢复确认、快照类型及清理/桌面规则数量；验证恢复仅变更保存的规则集合且不移动打开窗口。
- [x] 3.3 添加快照启用、固定间隔和自动保留数的调度控件；验证输入和持久化，并验证显示默认值为关闭、30 分钟和 3 个快照。
- [x] 3.4 更新托盘菜单命令，移除过时的具名布局快照创建、更新、复制和直接恢复入口；验证托盘导航仍打开规则快照管理，且没有旧操作可覆盖全局桌面规则匹配逻辑。
- [x] 3.5 为全局全部桌面规则、显式规则应用、定时快照、轮转、恢复不移动及旧快照限制添加中英文本地化和用户文档；验证本地化控件在窗体最小尺寸下不被裁剪。

## 4. 自动快照调度

- [x] 4.1 添加在规则状态服务初始化后启动且独立于窗体可见性的应用生命周期串行自动快照调度器；验证它在启动或启用后等待完整间隔且不补拍。
- [x] 4.2 捕获包含两个当前规则集合的自动快照，并在同一次状态更新中仅清理最早的自动快照；验证保留上限为 3 时第 4 份快照会移除最早自动快照但保留手动快照。
- [x] 4.3 设置编辑和配置导入后刷新或重新调度自动捕获，并记录可恢复捕获失败而不终止应用；验证变更间隔会重置下次执行，且并发手动/自动状态变更不会损坏历史。

## 5. 配置备份兼容性

- [x] 5.1 升级配置备份导出以包含统一当前规则状态和所有快照类型，同时省略临时窗口和桌面标识符；验证导出的填充备份包含清理规则、桌面规则和手动/自动/旧历史。
- [x] 5.2 升级导入验证和替换，以一致地恢复设置和统一规则状态、刷新活动界面和调度器状态，并保持打开窗口不变；验证导入新备份会恢复规则但在显式应用规则前不移动窗口。
- [x] 5.3 通过将旧布局快照转换为旧规则快照并在备份不含清理集合时保留本地清理锁定，支持先前便携备份版本；验证导入摘要/说明解释旧清理规则限制。

## 6. 集成验证

- [x] 6.1 在禁用发布后生成事件的情况下构建 `Debug | Any CPU`，并解决本次变更引入的全部编译错误和警告。
- [x] 6.2 在受支持的 Windows 10/11 上手工验证：跨多桌面的全局规则同步、正则保留、歧义匹配安全、显式应用预览与移动、手动恢复不移动、自动间隔捕获/轮转、迁移以及新旧备份往返。
- [x] 6.3 使用仓库规定的流程重启 Debug `WindowsVirtualDesktopHelper` 进程，确认新的响应中进程正在运行，并验证托盘管理器图标或 `Alt + D` 打开包含新规则操作的 `所有窗口` 概览。
